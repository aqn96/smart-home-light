# JWT Authentication
# Generate a secure key with: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-secret-key-here-change-this-to-random-string

# Database
DATABASE_URL=sqlite:///./smart_light.db

# Server Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=5173

# CORS (add your Pi's IP address)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional: Production Settings
# ENVIRONMENT=production
# LOG_LEVEL=info
